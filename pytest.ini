[pytest]
markers =
    db_type: marks tests for specific database types (e.g., postgresql, mysql)
    api_version: marks tests for specific API versions (e.g., v1, v2, v3)
    user_role: marks tests for specific user roles (e.g., admin, editor, guest)